import { AvailableCabModel } from './available-cab-model.model';

describe('AvailableCabModel', () => {
  it('should create an instance', () => {
    expect(new AvailableCabModel()).toBeTruthy();
  });
});
